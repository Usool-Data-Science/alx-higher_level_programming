#include <stdio.h>
#include "lists.h"

/**
  * is_palindrome - A function to check if head is palindrome
  * @head: The entry point of the list.
  *
  * Return: 0 if not and 1 if its a palindrom
  */

int is_palindrome(listint_t **head)
{
	if (*head == NULL || head == NULL)
		return (1);
	else
		return (0);
}
